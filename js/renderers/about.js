export function renderAbout() {
  return `
    <h2>About</h2>
    <p>This site provides probability calculators for board game components such as drawing cards with certain symbols or rolling dice combinations.</p>
    <p>All data is community-contributed JSON files. The structure is intentionally modular to allow future dynamic data sources (e.g. automated BoardGameGeek metadata fetch).</p>
  `;
}